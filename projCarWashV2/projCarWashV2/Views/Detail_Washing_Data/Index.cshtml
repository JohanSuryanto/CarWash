@model IEnumerable<projCarWashV2.Detail_Washing_Data>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Transaction_Data.Number_Of_Vehicles)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Price.Name_Price)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Transaction_Data.Number_Of_Vehicles)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Price.Name_Price)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID_Detail }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID_Detail }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID_Detail })
        </td>
    </tr>
}

</table>
